<div style="text-align: center;">
    <form method="post" action="{{ path('app_artisan_delete', {'id': artisan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline-block;">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artisan.id) }}">


        <button class="btn" style="background-color: white  ; color: black;">Delete</button>

    </form>
</div>